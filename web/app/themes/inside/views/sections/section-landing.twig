<div class="container landing">
  <h1 class="title mt-5 mb-4">Bienvenue à <u>Inside.eikon</u> !</h1>
  <div class="mb-5">
    <p class="description">
      {{ post.content }}
    </p>
  </div>
  {% if post.get_field('video_livefeed') %}
    <a href="#livefeed" class="jpo_btn">Vous êtes venus pour le live ? C'est par ici !  →</a>
  {% endif %}
</div>
